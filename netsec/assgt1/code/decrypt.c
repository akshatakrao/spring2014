/*
  decryptedContents = malloc(512);

  gcry_cipher_setiv(handle, &IV, 16);
	errorCode = gcry_cipher_decrypt(handle, decryptedContents, 512, encryptedContents, 512);

  printf("\n After Decryption: %s\n", decryptedContents);
		*/

